import { makeStyles } from '@material-ui/core/styles';


const Style = makeStyles((theme) => ({
    flex1: {
        flex: 1,
    },
    iconButton: {
    },
    selectionDiv: {
        zIndex: '1'
    }
}));

export default Style