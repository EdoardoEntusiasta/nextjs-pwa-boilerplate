import { makeStyles } from '@material-ui/core/styles';


const Style = makeStyles((theme) => ({
    formControl: {
        zIndex: 1
    },
    selectionDiv: {

    },
    iconButton: {
        
    }
}));

export default Style